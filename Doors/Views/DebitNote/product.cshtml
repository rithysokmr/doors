@model Doors.Models.DebitProductModel

<form id="form">
    <div class="form-group">
        <span>abc:</span>
        @Html.EditorFor(model => model.debit_full_id, new { htmlAttributes = new { @class = "form-control" }, @readonly = "readonly" })
    </div>
    <div class="form-group">
        <span>abc:</span>
        @Html.EditorFor(model => model.extra, new { htmlAttributes = new { @class = "form-control" }, @readonly = "readonly" })
    </div>
    <div class="form-group">
        <a href="#" class="btn btn-block btn-danger" id="SaveProduct">Save</a>
    </div>
</form>
           

@{
    <script>
        function AddProducts() {
            $("#ModalTitle").html("Add New Product");
            $("#products").modal();
        }
    </script>
}